/**
 * Example for a predefined decorator in the target TypeScript codebase.
 * @param name Name for a type in the example decorator.
 */
export function typeName(name: string) {
    return function (target) {}
}